PROMPT: UPDATE SINGLE-PAGE SITE – ESTEVEPANNETIER.COM (MINIMAL COPY REFRESH)

Objective
Update the copy of the existing single-page site (estevepannetier.com) with minimal yet precise edits to improve clarity and readability, while keeping:

The same structure (one page, multiple sections)

The same navigation items (anchor links)

The same services, offerings, and prices

The same projects and tools

This is a copy refresh, not a design overhaul.

Global Guidelines

Maintain the existing one-page layout and section order.

Keep nav labels: Who I'm For · Services · How I Work · Projects · Contact.

Replace current text with the new copy provided below, section by section.

Use short sentences, readable on mobile.

Tone: clear, warm, calm, intelligent, slightly poetic but not abstract.

Remove or avoid jargon where possible, while preserving key terms (ACX, agents, etc.) where they are important to the content.

Add “subtext” lines under personas and offers as indicated. These can be visually styled as small, low-contrast secondary text.

Section-by-section Copy to Insert

Use all the copy under “Single-page copy” above as the source of truth, mapping it to these structural blocks:

Hero → Label, Headline, Sub-headline, Body, CTA

Who I Work With → Section heading, intro sentence, persona cards + subtexts

Communication is a Lifelong Journey → Heading, subheading, age-stage card (existing carousel stays)

When? / AI Complexity Levels → Heading, 3-level explanation, legend, schema definitions

Three Ways to Work Together → Section heading, subheading, 3 offer cards (with new subtexts)

How I Approach AI → Section heading, 60/30/10 parts, summary paragraph

How I Work → Section heading, subheading, 4-step process text

Selected Clients & References → Section heading, intro line, sector headings and org lists unchanged

Ethical Stance → Section heading, quote, bullets

AI-Human Experience (ACX) Commitment → Subheading and bullets unchanged except line-breaks

Let’s Design Your Conversation → Section heading, short body, CTAs

Estève’s Kitchen → Section heading, intro, project tiles (keep existing copy, only adjust typos if any)

Insights on Communication → Section heading, subheading, 3 post teasers unchanged

AI & Authenticity Note → Intro paragraph + invitation to virtual coffee

Footer → Legal links, tagline line, name

Implementation Notes

All headings and CTAs should remain visually similar; this is text-only.

Ensure anchor links in the top navigation still scroll to the correct sections after text replacement.

Check line breaks and spacing for the longer sections (AI complexity, How I Approach AI, Kitchen projects).

Keep existing visual elements (icons, hand-drawn dividers, images) unchanged.

After implementing, do a pass on mobile to avoid text crowding in the persona cards and pricing cards.